# AG-UI 适配决策记录（Phase 8 §0.2 决策树）

> 日期：2026-08-13 · Owner：待指定 · 决策：**适配 POC**

## 决策树走查（沿用 Phase 3 §3.2）

| 判断点 | 结论 |
| :--- | :--- |
| 协议是否公开可读、无授权障碍 | 是：AG-UI 为开放协议（事件驱动，JSON 文档） |
| 是否能在不污染 L1/L2 的前提下封装 | 是：独立包 `@chameleon-ui/adapter-ag-ui`，协议分支只在 L3/L4 |
| 是否能在阶段内做出**可断言**的最小演示 | 是：双向状态同步 + 断言测试（本包） |
| 是否存在阻断性缺口 | 无阻断；全量 16 种事件类型未实现（见范围声明） |

→ 结论：**适配 POC**。不建空包；本包内含可运行实现 + 断言测试。

## 实现范围（诚实声明）

| 能力 | 状态 |
| :--- | :--- |
| STATE_SNAPSHOT / STATE_DELTA（RFC 6902 子集）双向同步 | 已实现，断言测试覆盖 |
| 断线恢复（reconnect 全量重同步） | 已实现 |
| 回环防护（远端应用不再回发） | 已实现 |
| 渲染指令 → install-core 安装计划（source=`ag-ui`） | 已实现 |
| 文本消息流 / 工具调用事件 / Run 生命周期 | **未实现**（POC 范围外） |
| 与 AG-UI 参考实现的线上互操作认证 | **未做**，不宣称兼容认证 |
| 传输层（WebSocket/SSE） | 进程内内存传输；宿主可自行桥接相同事件形状 |

## 复评

协议成熟度复评日期：随 Phase 9 硬化评审一并进行（日历待指定）。
