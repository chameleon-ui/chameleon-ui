# @chameleon-ui/components

L2 · React 组件主包（P0）。

Phase 0 空壳标记：`status: pending-M0`。M0 只保留正式迁入点，不在本包提前实现 POC 组件。

- 依赖：primitives · tokens · themes · i18n · contract  
- 单组件 DoD：实现 + 样式 + contract + locales + 测试  
- 目录：`src/<kebab-name>/`，导出 PascalCase
