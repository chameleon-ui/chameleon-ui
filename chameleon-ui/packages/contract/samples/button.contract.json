{
  "schemaVersion": "0.2",
  "slug": "button",
  "name": "Button",
  "status": "draft",
  "purpose": "Triggers a user-initiated action with a clear accessible name.",
  "scenarios": [
    "Submit a form",
    "Open a dialog",
    "Confirm or cancel a reversible action"
  ],
  "props": {
    "disabled": {
      "type": "boolean",
      "required": false,
      "description": "Prevents interaction while preserving semantic disabled state.",
      "default": false
    },
    "onPress": {
      "type": "event",
      "required": false,
      "description": "Runs after an enabled button is activated."
    }
  },
  "variants": [
    {
      "name": "variant",
      "values": ["solid", "outline"],
      "default": "solid",
      "description": "Controls visual emphasis without changing semantics."
    },
    {
      "name": "size",
      "values": ["sm", "md"],
      "default": "md",
      "description": "Controls density while retaining the target-size requirement."
    }
  ],
  "states": [
    { "name": "default", "description": "Enabled and idle." },
    { "name": "disabled", "description": "Unavailable for interaction." }
  ],
  "composition": {
    "allowedParents": ["form", "dialog", "toolbar"],
    "allowedChildren": ["text", "icon"],
    "requiredContext": []
  },
  "antiPatterns": [
    "Do not use for navigation when a semantic link is appropriate.",
    "Do not hide the accessible name behind an unlabeled icon."
  ],
  "a11y": {
    "role": "button",
    "keyboard": ["Tab focuses", "Enter activates", "Space activates"],
    "focus": "Shows a visible focus indicator and remains in document order.",
    "labeling": "Has visible text or an explicit accessible name.",
    "wcag": ["2.1.1", "2.4.7", "4.1.2"]
  },
  "responsive": {
    "strategy": "Keep behavior and semantics stable while preserving a usable target.",
    "breakpoints": {
      "compact": "May grow to fill a narrow action region.",
      "medium": "Uses content width by default.",
      "large": "Uses content width by default."
    }
  },
  "platforms": {
    "web": "supported",
    "react": "planned",
    "vue": "planned"
  },
  "rtl": {
    "supported": true,
    "strategy": "Use logical CSS properties; do not mirror text or symmetric icons.",
    "mirroredValues": ["directional-icon"]
  },
  "dataAi": {
    "role": "button",
    "states": ["disabled"],
    "intents": ["submit", "confirm", "cancel"]
  },
  "telemetry": {
    "hooks": []
  }
}
