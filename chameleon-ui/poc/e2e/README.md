# @chameleon-ui/poc-e2e

Phase 0 真浏览器全检：对 Ark / Base 两条 POC 预览站跑完整 locale × direction 矩阵，不再用人工抽检代替。

覆盖：`en` / `en-XA`、`ltr` / `rtl`、Button / Input / Dialog、390 / 768 / 1280、Portal 计算方向、禁止 `data-ai-*`。
