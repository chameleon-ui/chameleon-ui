---
id: themes
title: 主題
sidebar_label: 主題
description: 來自 @chameleon-ui/themes 的 8 套官方致敬主題。
---

import ThemeList from '@site/src/components/ThemeList'

來自 `@chameleon-ui/themes` 的 overlay（8 套官方致敬主題，免費）。項目擁有者已確認可作官方主題出貨；禁止宣稱可被認出的比例。社區市場上架可以免費或付費。

<ThemeList />

使用導航欄主題切換。overlay 作用在文檔根節點的 `data-theme` 上。

應用裡載入 overlay CSS 請走包 `exports`，不要猜檔案系統路徑：

```ts
import "@chameleon-ui/themes/cupertino/css";
```

`import "@chameleon-ui/themes/dist/cupertino/variables.css"` 同樣有效（`exports["./dist/*"]`）。

