import { TabsPreview } from '@site/src/playgrounds'

## Examples

<TabsPreview />

```jsx live
function Example() {
  return (
    <Tabs
      defaultValue="one"
      items={[
        { value: 'one', label: 'Overview', content: 'First panel' },
        { value: 'two', label: 'Specs', content: 'Second panel' },
      ]}
    />
  );
}
```
