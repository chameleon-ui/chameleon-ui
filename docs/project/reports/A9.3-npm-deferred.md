# A9.3 · npm 首发 — **deferred**（2026-08-15）

> **Decision:** do **not** run `npm publish` for `0.1.9`.  
> **Owner decision:** 先不上架（product cut exists; registry upload deferred).  
> **Technical blocker (if retry later):** E403 — 2FA or granular token with bypass 2FA required.  
> **A9.3 status:** **deferred** — not failed inventively, not claimed published.

## What is ready in-repo

| Item | State |
| :--- | :--- |
| Product version | `0.1.9` (workspace aligned) |
| Git tag | `v0.1.9` |
| `publish:check` | Dry-run green (`npmPublish: false`) |
| Would-publish set | 19 packages incl. `@chameleon-ui/react` / `@chameleon-ui/vue` umbrellas |
| `npm view @chameleon-ui/tokens version` | **404** (unpublished) |

Evidence: [`2026-08-15-npm-publish-blocker.md`](./2026-08-15-npm-publish-blocker.md) · [`2026-08-15-release-0.1.9.md`](./2026-08-15-release-0.1.9.md).

## What must not be claimed

- 「已 npm 发布」「registry 可 `npm install @chameleon-ui/*`」
- Provenance / 三通道干净环境复跑取证（A9.3 remainder）
- Updating AGENTS / consume docs to prefer registry over link/pack

## External consume until publish

- Umbrella preferred: `@chameleon-ui/react` / `@chameleon-ui/vue` via `link-external` / `pack-external` / official templates
- Source zip: `pnpm pack:source` → `dist-release/chameleon-ui-0.1.9-source.zip`

## Unblock path (owner)

1. Explicit go-ahead to 上架 (override 先不上架), **and**
2. npm 2FA or granular token with bypass 2FA, **and**
3. Confirm `@chameleon-ui` org/scope ownership, **then**
4. `pnpm publish -r --access public` + `npm view` proof in a new report.

Until then A9.3 stays **deferred**. `phase9:gates` only runs `publish:check` dry-run.
