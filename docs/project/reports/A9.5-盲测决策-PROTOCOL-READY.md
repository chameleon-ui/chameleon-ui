# A9.5 盲测决策（0.1.9 commercial-essentials）

> **Decision: `PROTOCOL-READY`.** Not a waive. Not a completed run.  
> Machine-readable: [`A9.5-decision.json`](./A9.5-decision.json).  
> Pending result: [`盲测结果.pending.json`](./盲测结果.pending.json) (`status=not_run`, `rate=null`).  
> Protocol: [`盲测协议.md`](./盲测协议.md).  
> Owner: **待指定**. Legacy: **LEGACY-2026-008**.

## 1. What A9.5 requires

From [`Phase-9-硬化与发布.md`](../phases/Phase-9-硬化与发布.md) A9.5:

> 盲测：方法与结果公布（样本量可查），**或**书面放弃函；README 不出现「≥80%」无证据表述。

## 2. What was searched

| Candidate | Found? |
| :--- | :--- |
| Owner-signed waive letter for 「一眼认出 ≥80%」 | **No** — no waive / 放弃函 file in-repo |
| Aggregated human `盲测结果.json` with real `trials[]` | **No** — only pending placeholder |
| In-demo harness `/?view=blind` + protocol | **Yes** |

Because no signed waive exists, **0.1.9 does not document waive**. Default for this slice: **PROTOCOL-READY**.

## 3. Decision (binding for commercial storytelling)

| Field | Value |
| :--- | :--- |
| `decision` | **`PROTOCOL-READY`** |
| `waive` | `false` |
| Human run | **not_run** |
| Published recognition `rate` | **`null`** (must stay null until trials aggregate) |
| 「一眼认出 ≥80%」 commercial / README / site claim | **Forbidden** |
| PHASE2 / PHASE3 / PHASE9 ≥80% checkbox | **Remain unchecked** |

## 4. How a human produces a real result (no invented %)

1. Start demo: `corepack pnpm@9.15.0 demo` → open `http://127.0.0.1:5175/?view=blind`.
2. Follow [`盲测协议.md`](./盲测协议.md) §§2–5.
3. Export JSON from the harness (copy/download). Default filename `盲测结果.json`.
4. Save under `docs/project/reports/` only after a **real** session. `rate` must be computed from `trials[]`, never typed by hand.
5. Only then may A9.5 move from PROTOCOL-READY toward complete (or a separate signed waive letter).

## 5. Theme quantification (Worker B)

Engineering theme measurables (S3 / design-rules / tokens) are **not** recognition rates. Report: [`2026-08-15-theme-quantification.md`](./2026-08-15-theme-quantification.md) (`recognition_rate=null`). **Never** substitute theme KB numbers for ≥80% recognition.

## 6. Explicit non-claims

- No recognition rate ≥80% (or any percent) is published.
- Harness + protocol ready ≠ blind test done.
- This decision is **not** a waive letter and does **not** close LEGACY-2026-008.
