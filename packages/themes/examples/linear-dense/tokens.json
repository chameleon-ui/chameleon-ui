{
  "$comment": "Phase 8 §3.7 derived-theme demo: inherits `linear` via DTCG $extends and stores ONLY the delta (差量存储). Not one of the 8 official themes; not in the registry.",
  "$extends": "../../src/linear/tokens.json",
  "radius": {
    "sm": { "$value": { "value": 1, "unit": "px" } },
    "md": { "$value": { "value": 2, "unit": "px" } },
    "lg": { "$value": { "value": 4, "unit": "px" } }
  }
}
