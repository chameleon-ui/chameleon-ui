/**
 * @chameleon-ui/utils — image helpers.
 *
 * Generic, framework-free PNG/image utilities. See `png.ts` for the core
 * primitives (decode / encode / clear-region-to-transparent / opaque-bounds).
 *
 * Compliance note: these are neutral image-processing primitives. They are not
 * a watermark-removal feature and must only be used on assets you own or are
 * licensed to modify.
 */

export * from './png.js';
