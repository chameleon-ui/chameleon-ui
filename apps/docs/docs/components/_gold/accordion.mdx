## 示例

```jsx live
function Example() {
  return (
    <Accordion
      items={[
        { title: '安装', content: '使用 chameleon add accordion。' },
        { title: '契约', content: 'API 表由 contract.json 生成，不要手抄。' },
        { title: '无障碍', content: '每个触发器使用 aria-expanded。' },
      ]}
    />
  );
}
```

允许多开：

```jsx live
function Example() {
  return (
    <Accordion
      multiple
      items={[
        { title: '主题', content: '8 套官方致敬主题，用导航栏切换。' },
        { title: '语言', content: '文档站三语；产品 ICU 仍为 21 语。' },
      ]}
    />
  );
}
```
