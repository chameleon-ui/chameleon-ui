import { InputPlayground } from '@site/src/playgrounds'

## Examples

<InputPlayground />

```jsx live
function Example() {
  const [value, setValue] = useState('Chameleon');
  return <Input label="Project name" value={value} onChange={setValue} />;
}
```

```jsx live
function Example() {
  const [email, setEmail] = useState('');
  return (
    <Input
      label="Email"
      value={email}
      onChange={setEmail}
      invalid={!email.includes('@')}
      errorMessage="Enter a valid email address"
    />
  );
}
```
